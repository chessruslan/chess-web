[build]
  command = "flutter build web --release --no-tree-shake-icons --dart-define=SUPABASE_URL=$SUPABASE_URL --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY"
  publish = "build/web"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
